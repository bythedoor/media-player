{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run wxWidgets App (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/app",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build-app"
    }
  ]
}
